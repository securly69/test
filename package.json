{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "A simple proxy server using Express and http-proxy-middleware",
  "main": "proxy-server.js",
  "scripts": {
    "start": "node proxy-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^3.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
